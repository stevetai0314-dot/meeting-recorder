# 外銷部會議錄音工具 V3 — 一次性設定指南

照順序做，每步都寫了畫面上實際點哪裡。全部做完後，以後每天只要：開 recorder.html → 錄音 → 按上傳分析。

---

## 第 1 步：申請 Gemini API 金鑰（免費）

1. 用 Chrome 開 **aistudio.google.com**，登入 Google 帳號
2. 點左上角（或右上角）的「**Get API key**」（取得 API 金鑰）
3. 點藍色按鈕「**建立 API 金鑰**」（Create API key）
4. 出現一串 `AIza` 開頭的金鑰 → 點旁邊的**複製**圖示，先貼到記事本暫存

> 免費額度：每天可處理的量遠超過「一天一場 40 分鐘會議」，不用擔心費用。

## 第 2 步：建立 GAS 專案

1. 開 **script.google.com** → 點左上「**新專案**」
2. 把左側檔案 `程式碼.gs` 裡的內容全部刪掉，貼上本專案 `gas/Code.gs` 的全部內容
3. 點上方**磁碟片圖示**儲存（或 Ctrl+S）
4. 點左上角「未命名專案」改名為：**外銷部會議記錄工具**

## 第 3 步：設定金鑰與時區

1. 點左側**齒輪圖示**「專案設定」
2. 勾選「**在編輯器中顯示 "appsscript.json" 資訊清單檔案**」
3. 回到左側**編輯器**（< > 圖示），點開 `appsscript.json`，內容全部換成本專案 `gas/appsscript.json` 的內容，儲存
4. 再回到齒輪「專案設定」，拉到最下面「**指令碼屬性**」→ 點「**新增指令碼屬性**」
   - 屬性：`GEMINI_API_KEY`
   - 值：貼上第 1 步複製的金鑰
   - 點「儲存指令碼屬性」

## 第 4 步：測試後端（在 GAS 編輯器裡）

1. 回到編輯器，上方工具列有個函式下拉選單（「偵錯」旁邊）
2. 選 **testBuildMarkdown** → 按「**執行**」
   - 第一次會跳「需要授權」→ 點「審查權限」→ 選你的帳號 →
     出現警告畫面時點「**進階**」→ 點「**前往 外銷部會議記錄工具（不安全）**」→ 點「**允許**」
   - 下方「執行紀錄」應顯示 MD 內容和 `testBuildMarkdown PASS`
3. 選 **testGeminiKey** → 執行 → 執行紀錄應顯示「OK」
   - 如果顯示「尚未設定 GEMINI_API_KEY」→ 回第 3 步檢查屬性名稱有沒有打錯
4. 選 **testUploadSkeleton** → 執行 → 應顯示 `{ok=true, ...}` 和表頭七欄
   - 執行完你的雲端硬碟會多出「外銷部會議錄音」資料夾和「外銷部會議記錄」試算表——這是正常的，以後就用它們

## 第 5 步：部署成網頁應用程式

1. 點右上角藍色「**部署**」→「**新增部署作業**」
2. 點左上角**齒輪圖示** → 選「**網頁應用程式**」
3. 設定：
   - 說明：隨便填（例如 v3）
   - 執行身分：「**我**」
   - 誰可以存取：「**所有人**」
4. 點「部署」→ 複製「**網頁應用程式**」下方那串 `https://script.google.com/macros/s/…/exec` 網址

> 之後如果改了 Code.gs，要點「部署」→「**管理部署作業**」→ 鉛筆圖示 → 版本選「**新版本**」→ 部署，網址不變。

## 第 6 步：把網址填回前端

1. 用記事本開啟 `recorder.html`
2. 找到 `const GAS_URL = '請貼上GAS部署網址';`
3. 把引號裡換成第 5 步複製的 `/exec` 網址，儲存

## 第 7 步：Google Drive 桌面版同步（讓 MD 自動進 Obsidian）

1. 下載安裝 **Google Drive 桌面版**：google.com/drive/download
2. 登入後，點工作列的 Drive 圖示 → 齒輪「**偏好設定**」→ 左側「**Google 雲端硬碟**」→ 選「**鏡像檔案**」(Mirror files)
   - 記下它顯示的本機資料夾位置（通常是 `C:\Users\STEVE\我的雲端硬碟` 或 `G:\我的雲端硬碟`）
3. 等同步完成後，確認該位置底下有「**會議記錄**」資料夾
   （如果還沒有，先用工具跑一次完整流程，GAS 會自動建立）
4. 以**系統管理員**身分開 PowerShell（開始鍵 → 輸入 powershell → 右鍵「以系統管理員身分執行」），執行：

   ```powershell
   New-Item -ItemType Junction -Path "C:\Users\STEVE\Documents\Obsidian Vault\會議記錄" -Target "C:\Users\STEVE\我的雲端硬碟\會議記錄"
   ```

   （`-Target` 改成第 2 點記下的實際路徑 + `\會議記錄`）
5. 開 Obsidian → 左側檔案列表應出現「會議記錄」資料夾

> 不想裝 Drive 桌面版的備援做法：每天到 drive.google.com 開「會議記錄」資料夾，把 MD 下載後拖進 Vault。

---

## 日常使用

1. 開 `recorder.html` → 按「開始錄音」→ 選整個螢幕、**勾分享系統音訊** → 開會
2. 會議結束按「停止錄音」→（選填備註）→ 按「**☁ 上傳分析**」
3. 等 2～4 分鐘，頁面顯示摘要 → 完成
   - 逐字稿在 Google Sheet「外銷部會議記錄」
   - 會議筆記自動出現在 Obsidian「會議記錄」資料夾，客人名可點 `[[ ]]` 連結追蹤歷次記錄

## 常見問題

- **顯示「尚未設定 GAS 網址」**：第 6 步沒做，或存檔後沒重新整理頁面
- **顯示「Gemini API 錯誤 429」**：當天免費額度用完（極少發生），隔天再按重試
- **等超過 5 分鐘顯示失敗**：後端可能其實已完成，先去 Sheet 看有沒有新的一列再決定重試
- **Sheet 出現「待重新分析」列**：分析失敗但 MP3 已保存，修正問題後在頁面重按「上傳分析」（不會重複上傳檔案）
